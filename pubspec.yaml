name: exposure_documentation
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_signature_pad: ^3.0.1
  cupertino_icons: ^1.0.6
  pdf: ^3.10.8
  path_provider: ^2.1.3
  google_fonts: ^6.2.1
  flutter_to_pdf: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
